<section class="relative flex w-auto h-auto">
  <video
    class="max-w-full max-h-screen aspect-[9/16]"
    autoplay
    loop
    muted
    playsinline
    preload="auto"
  >
    <source src="/video/IMG_4879.mp4" type="video/mp4" />
  </video>
  <div
    class="z-5 flex justify-center items-center w-full min-h-full top-0 left-0 absolute animate-fade-left delay-300 duration-200 transition"
  >
    <h2 class="borde">ZMR</h2>
    <h2 class="wave">ZMR</h2>
  </div>
</section>
<style>
  h2 {
    font-family: "Voga";
    font-size: 25vmin;
    position: absolute;
  }
  .borde {
    color: #603721;
    text-shadow:
      -1px -1px 0 white,
      1px -1px 0 white,
      -1px 1px 0 white,
      1px 1px 0 white;
  }
  .wave {
    color: #e7d4c5;
    animation: shift 3s ease-in-out infinite;
  }

  @keyframes shift {
    0%,
    100% {
      clip-path: polygon(
        0% 47%,
        10% 48%,
        33% 54%,
        54% 60%,
        70% 61%,
        84% 59%,
        100% 52%,
        100% 100%,
        0% 100%
      );
    }
    50% {
      clip-path: polygon(
        0% 60%,
        15% 65%,
        34% 66%,
        51% 62%,
        67% 50%,
        84% 45%,
        100% 46%,
        100% 100%,
        0% 100%
      );
    }
  }
</style>
